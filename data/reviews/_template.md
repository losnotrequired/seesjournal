---
title:
exhibition:
artist:
venue:
neighborhood:
dates:
author:
date: 2026-01-01
dek:
originality: 0
installation: 0
concept: 0
historical_importance: 0
emotional_impact: 0
image:
status: draft
---
Write the review here in Markdown. Blank lines separate paragraphs.

Use `## Subhead` for a section break, `> a line` for a pull quote, `**bold**` and
`*italic*` for emphasis, and `[link text](https://example.com)` for links.

When the review is ready, change `status` above to `published` and run
`python gen_reviews.py`. Files whose name starts with `_` (like this one) are never built.
